{
  "name": "tagged-string",
  "version": "1.1.2",
  "description": "Extract structured data from strings using tag-based syntax",
  "license": "MIT",
  "author": "tbeseda",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tbeseda/tagged-string.git"
  },
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc && tsc -p tsconfig.test.json",
    "lint": "biome check",
    "lint:fix": "biome check --write",
    "prepack": "npm run build",
    "pretest": "npm run typecheck",
    "test": "node --test 'test/**/*.test.ts'",
    "posttest": "npm run lint",
    "examples": "node src/examples.ts",
    "release": "npm run build && npx release-it"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.13",
    "@types/node": "^26.0.0",
    "typescript": "^7.0.2"
  }
}
